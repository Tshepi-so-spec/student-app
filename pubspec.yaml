name: student_assistant_app
description: TPG316C - Student Assistant Application System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  provider: ^6.1.2
  file_picker: ^8.0.0
  uuid: ^4.3.3
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.6
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
